# CSCMS-Photo-Automation
Photo automation pipeline using Open AI API for photo recognition and python script for photo resize




Terminal Enviorment (run these):

source .venv/bin/activate

python src/pipeline.py --dry-run --limit 3